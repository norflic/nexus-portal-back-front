info:
  name: read all (8)
  type: http
  seq: 5
  tags:
    - Tag

http:
  method: GET
  url: "{{baseUrl}}/tags"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/tags"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
