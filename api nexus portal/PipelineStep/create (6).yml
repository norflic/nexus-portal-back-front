info:
  name: create (6)
  type: http
  seq: 1
  tags:
    - PipelineStep

http:
  method: POST
  url: "{{baseUrl}}/pipelinestep"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/pipelinestep"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
