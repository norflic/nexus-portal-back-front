info:
  name: read by user id
  type: http
  seq: 5
  tags:
    - PipelineStep

http:
  method: GET
  url: "{{baseUrl}}/pipelinestep/uid/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/pipelinestep/uid/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
