info:
  name: read all (6)
  type: http
  seq: 6
  tags:
    - PipelineStep

http:
  method: GET
  url: "{{baseUrl}}/pipelinesteps"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/pipelinesteps"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
