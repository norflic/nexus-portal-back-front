info:
  name: getCascadeByMonth
  type: http
  seq: 8
  tags:
    - Defense

http:
  method: POST
  url: "{{baseUrl}}/defenses/getByMonthCascade"
  body:
    type: json
    data: |-
      {
        "data": {
          "year": 2026,
          "month": 3
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/defense"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
