info:
  name: read all (9)
  type: http
  seq: 5
  tags:
    - UserType

http:
  method: GET
  url: "{{baseUrl}}/usertypes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/usertypes"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
