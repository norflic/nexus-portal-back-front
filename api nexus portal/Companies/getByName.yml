info:
  name: getByName
  type: http
  seq: 1

http:
  method: GET
  url: companies/search/?name=a
  params:
    - name: name
      value: a
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
