info:
  name: create (2)
  type: http
  seq: 1
  tags:
    - CursusType

http:
  method: POST
  url: "{{baseUrl}}/cursustype"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/cursustype"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
