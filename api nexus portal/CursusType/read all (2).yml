info:
  name: read all (2)
  type: http
  seq: 5
  tags:
    - CursusType

http:
  method: GET
  url: "{{baseUrl}}/cursustypes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/cursustypes"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
