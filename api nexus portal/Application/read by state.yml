info:
  name: read by state
  type: http
  seq: 5
  tags:
    - Application

http:
  method: GET
  url: "{{baseUrl}}/application/state/:state"
  params:
    - name: state
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/application/state/:state"
      method: GET
      params:
        - name: state
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
