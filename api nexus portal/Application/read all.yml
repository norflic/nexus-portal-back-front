info:
  name: read all
  type: http
  seq: 6
  tags:
    - Application

http:
  method: GET
  url: "{{baseUrl}}/applications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/applications"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
