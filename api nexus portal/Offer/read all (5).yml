info:
  name: read all (5)
  type: http
  seq: 5
  tags:
    - Offer

http:
  method: GET
  url: "{{baseUrl}}/offers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/offers"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
