info:
  name: read all (GET)
  type: http
  seq: 5
  tags:
    - Company

http:
  method: GET
  url: "{{baseUrl}}/companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/companies"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
