info:
  name: update (1)
  type: http
  seq: 2
  tags:
    - ClassGroup

http:
  method: PUT
  url: "{{baseUrl}}/classgroup"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/classgroup"
      method: PUT
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
