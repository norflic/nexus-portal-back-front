info:
  name: read all (7)
  type: http
  seq: 5
  tags:
    - Preference

http:
  method: GET
  url: "{{baseUrl}}/preferences"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/preferences"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
